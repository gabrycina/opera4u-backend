{
  "routes": [
    {
      "method": "GET",
      "path": "/hero-images",
      "handler": "hero-images.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/hero-images/count",
      "handler": "hero-images.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/hero-images/:id",
      "handler": "hero-images.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/hero-images",
      "handler": "hero-images.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/hero-images/:id",
      "handler": "hero-images.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/hero-images/:id",
      "handler": "hero-images.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
